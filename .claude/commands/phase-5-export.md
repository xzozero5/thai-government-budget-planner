---
description: Phase 5 — PDF export + save/load JSON
---
เริ่ม Phase 5 ตาม `docs/05-FEATURES.md` F5 และ `docs/04-ARCHITECTURE.md` D6; BACKLOG T-501..T-503

1. `frontend-dev` ทำ T-501 (react-pdf + Sarabun; ตรวจผล spike S4 ใน `docs/decisions/SPIKES.md` ก่อน) → T-502
2. `qa-engineer` ทำ T-503 ตรวจ PDF ใน viewer หลายตัว (ใช้ `pdftotext`/`pdffonts` ช่วยตรวจ embed font และข้อความไทย)
3. STATUS/BACKLOG/commit `export: pdf + json`
