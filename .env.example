# web/.env.local (ห้าม commit) — ใช้เฉพาะรัน eval บนเครื่อง dev
VITE_EVAL_ANTHROPIC_API_KEY=
# (optional) base URL สำหรับลิงก์เปิดไฟล์ต้นทางจาก citation drawer (https หรือ same-origin เท่านั้น)
VITE_SOURCE_BASE_URL=
