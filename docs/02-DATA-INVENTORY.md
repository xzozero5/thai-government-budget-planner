# 02 — Data Inventory (สำรวจจริง 19 ก.ย. 2569)

ทุกอย่างในหน้านี้คือ **ยืนยันแล้ว** จากการเปิดไฟล์จริง เว้นแต่ติดป้าย `[UNVERIFIED]`
Root ข้อมูลดิบ: `./เพราะ AI ไม่ใช่แค่ CHATBOT/` — ขนาดรวม ≈ 3.2 GB, 318 ไฟล์ — นับจริงด้วย `tgbp inventory` 19 ก.ย. 2569: pdf 262 (261 ไฟล์ `.pdf` + 1 ไฟล์ไม่มีนามสกุลที่ magic bytes เป็น `%PDF`), xlsx 36, xls 13, jpg 4, pptx 1, docx 1, other 1 (`.DS_Store`); ตามชุด: committee 189, province_budget 111, pbo 12, open_sso 6; มีไฟล์เนื้อหาซ้ำกัน (sha1) 41 กลุ่ม — ส่วนใหญ่คือชุดร่าง พ.ร.บ. ฉบับเต็มที่ซ้ำระหว่างโฟลเดอร์เชียงใหม่/สมุทรปราการ (รายละเอียด: `docs/02-DATA-INVENTORY.appendix.md`, auto-generated)

Environment: (sandbox ตอนวางแผน) python3, pandas 2.3, openpyxl, xlrd, pypdf, pdfplumber, poppler, libreoffice, node 22 · **เครื่อง dev จริงของคุณนิว (Windows 11, ตรวจ 19 ก.ย.)**: Python 3.13.2, node 22.15, uv ผ่าน `python -m uv`; **ไม่มี `pdfinfo` และไม่มี LibreOffice** → pipeline ใช้ Python ล้วน (pypdf/pdfplumber, xlrd) ไม่พึ่ง CLI ภายนอก

## A. ชุดข้อมูลหลัก (structured — ใช้สร้าง "ราคาในอดีต")

### A1. `PBO/25xx.xlsx` — ผลเบิกจ่ายรายรายการ ปีงบ 2558–2568 (11 ไฟล์, 20–55 MB/ไฟล์)

**ชุดข้อมูลที่มีค่าที่สุด** — ระดับ "ชื่อรหัสงบประมาณ" (item) พร้อม พ.ร.บ. / หลังโอน / PO / เบิกจ่ายจริง

| ปี | sheet ข้อมูล | rows (รวม header และ Grand Total ถ้ามี) | หมายเหตุ |
|---|---|---|---|
| 2558 | `เบิกจ่ายภาพรวมทุกมิติ (8)` | 173,988 | |
| 2559 | `เบิกจ่ายภาพรวมทุกมิติ (13)` | 250,476 | |
| 2560 | `เบิกจ่ายภาพรวมทุกมิติ (14)` | 229,118 | |
| 2561 | `เบิกจ่ายภาพรวมทุกมิติ (15)` | 219,247 | **มี `Sheet1` (pivot 35 แถว) นำหน้า — ต้องเลือกชีตด้วยชื่อ prefix**; **ไม่มีแถว Grand Total** (ข้อมูลเริ่มแถว 2); `Sheet1` = pivot `ผลรวม ของ พรบ.` รายกระทรวง แถว `ผลรวมทั้งหมด` = 3,050,000.007 ล้านบาท → ใช้เป็น oracle V1 |
| 2562 | `เบิกจ่ายภาพรวมทุกมิติ (3)` | 217,120 | **ไฟล์ต้นทางไม่ครบ**: ผลรวมแถว ≈ 2,377,340 จาก Grand Total 3,000,000 ล้านบาท (≈ 79 %) — ไม่มีแถวของ ก.การคลัง, กลาโหม, การต่างประเทศ, พม., ท่องเที่ยวฯ, ชดใช้เงินคงคลัง; เกษตรฯ/สำนักนายกฯ มีบางส่วน; แถว 226–227 คอลัมน์เลื่อน → ดู ADR-004 |
| 2563 | `เบิกจ่ายภาพรวมทุกมิติ (4)` | 336,700 | |
| 2564 | `เบิกจ่ายภาพรวมทุกมิติ (5)` | 452,808 | ใหญ่สุด |
| 2565 | `เบิกจ่ายภาพรวมทุกมิติ (6)` | 356,758 | |
| 2566 | `เบิกจ่ายภาพรวมทุกมิติ (7)` | 188,802 | |
| 2567 | `เบิกจ่ายภาพรวมทุกมิติ (8)` | 221,572 | **max_column = 16384 (format bloat) — ต้อง trim ให้เหลือ 22 คอลัมน์แรก** (คอลัมน์ 23–30 มี header `คอลัมน์1`–`คอลัมน์8` ไม่มีข้อมูล); **ไม่มีแถว Grand Total และไม่มี Sheet1 → ไม่มี oracle ในไฟล์** |
| 2568 | `เบิกจ่ายภาพรวมทุกมิติ (8)` | 241,160 | **มี `Sheet1` นำหน้า** เหมือน 2561; **ไม่มีแถว Grand Total**; `Sheet1` = pivot เฉพาะ **สำนักนายกรัฐมนตรี** (ประจำ/ลงทุน × พรบ./หลังโอน/เบิกจ่าย; รวม พรบ. 35,747.449 ล้านบาท) → oracle V1 ได้แค่ระดับกระทรวงเดียว |

รวม ≈ **2.9 ล้านแถว**

คอลัมน์ (22 คอลัมน์ เหมือนกันทุกปี — ยืนยันแล้ว):
```
ปีงบประมาณ | กระทรวง | หน่วยงาน | ยุทธศาสตร์การจัดสรร | แผนงาน | ผลผลิต/โครงการ | งาน/โครงการ |
งบรายจ่าย | รายจ่ายประจำ/ลงทุน | ชื่อรหัสงบประมาณ |
พรบ. (ล้านบาท) | งบฯ หลังโอน/ปป. ทั้งสิ้น (ล้านบาท) | PO ทั้งสิ้น (ล้านบาท) | เบิกจ่ายทั้งสิ้น (ล้านบาท) |
เบิกจ่ายรวม PO (ล้านบาท) | เงินกันฯ สุทธิ (ล้านบาท) | เบิกจ่ายเหลื่อมปี (ล้านบาท) |
คงเหลือกรณีมีหนี้ผูกพัน | คงเหลือกรณีไม่มีหนี้ผูกพัน | คงเหลือ สรก.อยู่ระหว่างดำเนินการ |
คงเหลืออยู่ระหว่างกันและขยายรวม | คงเหลือรวม        ← 5 คอลัมน์ท้าย (18–22) หน่วยล้านบาท — ชื่อย่อจากรายงาน explorer; T-105 ต้อง log ชื่อ header แบบ exact string ลง appendix
```
ตรวจซ้ำ 19 ก.ย. 2569 (explorer + main thread สแกนทั้งชีตปี 2561/2567/2568): header คอลัมน์ 1–22 เหมือนกันทุกปี; `ปีงบประมาณ` เป็น int; Grand Total (คอลัมน์ `พรบ.` / `เบิกจ่ายทั้งสิ้น`, ล้านบาท) ของปีที่มี: 2558 2,575,000 / 2,378,260 · 2559 2,776,000 / 2,578,774 · 2560 2,923,000 / 2,686,563 · 2562 3,000,000 / 2,783,155 · 2563 3,200,000 / 2,943,851 · 2564 3,285,962 / 3,012,143 · 2565 3,100,000 / 2,932,563 · 2566 3,185,000 / 3,088,425 (ค่าปัด) — T-105 extract จริง 19 ก.ย.: รวม **2,887,730 แถว**; ผลรวมตรง oracle < 0.01 % ทุกปีที่มี oracle ยกเว้น 2562 (ADR-004); ปี 2567 (ไม่มี oracle) รวม พรบ. = 3,602,000; 2568 = 3,752,700 ล้านบาท; header คอลัมน์ 18–22 exact: `คงเหลือกรณีมีหนี้ผูกพัน (ล้านบาท)`, `คงเหลือกรณีไม่มีหนี้ผูกพัน (ล้านบาท)`, `คงเหลือ สรก.อยู่ระหว่างดำเนินการ (ล้านบาท)`, `คงเหลืออยู่ระหว่างกันและขยายรวม (ล้านบาท)`, `คงเหลือรวม (ล้านบาท)`
Gotchas ที่ยืนยันแล้ว:
- แถวที่ 2 เป็น `Grand Total` **เฉพาะปี 2558–2560 และ 2562–2566** → drop โดย **ตรวจเนื้อหา** (`'Grand Total'` ในคอลัมน์มิติ) ห้าม drop ตามเลขแถว — ปี **2561, 2567, 2568 ไม่มี Grand Total** (แถว 2 คือข้อมูลจริง; แก้จากที่เคยบันทึกผิด)
- ค่าว่างเป็นสตริง `'-'` → null
- หน่วยเงิน **ล้านบาท** (ทศนิยม 4 ตำแหน่ง) → แปลงเป็นบาท integer
- `ชื่อรหัสงบประมาณ` มักผสม **สเปค + จำนวน + สถานที่** เช่น
  `เครื่องปรับอากาศ แบบแยกส่วน (ราคารวมค่าติดตั้ง) แบบตั้งพื้นหรือแบบแขวน ขนาด 18,000 บีทียู สำนักงานพลังงานจังหวัดเชียงใหม่ ตำบลช้างเผือก อำเภอเมืองเชียงใหม่ จังหวัดเชียงใหม่`
  `ก่อสร้างลานกีฬา สนามฟุตซอล บ้านทุ่งฝูง หมู่ที่ 4 องค์การบริหารส่วนตำบลร่องเคาะ อำเภอวังเหนือ จังหวัดลำปาง`
  → ปี 2566: 188,801 แถว มี distinct item **165,518** (เกือบ unique ต่อแถว) และ distinct หน่วยงาน 735
  → **ต้องมีขั้น normalize**: สกัด `province/amphoe/tambon`, `qty + unit` ("1 เครื่อง", "จำนวน 2 คัน"), `spec tokens` (BTU, ตัน, ล้อ, ฟุต, นิ้ว, kW, ตร.ม., เมตร) แล้วสร้าง `item_key` ที่ตัดสถานที่ออก เพื่อ group เทียบราคาข้ามหน่วยงาน/ปี
- `PBO/ข้อมูลสถิติงบประมาณรายจ่าย (Dashboard).jpg` — รูป ไม่ใช้

### A2. ร่าง พ.ร.บ. งบประมาณ 2570 ฉบับเต็ม (Excel) — `งบประมาณ เชียงใหม่/1 - .../ร่าง พ.ร.บ. งบ 2570 ฉบับเต็ม - Excel.xlsx` (6.6 MB)
(ไฟล์เดียวกันซ้ำอยู่ใน `งบประมาณ สมุทรปราการ/1 - ...` — ให้ dedupe ด้วย hash)

- sheet `Data Dict` (13 ฟิลด์) + sheet `Data` **96,470 แถว**
- คอลัมน์: `min, min_name, agc, agc_name, group_budget, plan_name, output_name, act_name, objc, objc_8_name, cap_ncap, item_name, p_total_bud` (บาท, integer)
- คุณภาพดี มีรหัสกระทรวง/กรม → ใช้เป็น master ของชื่อหน่วยงานได้
- **ยืนยันจากไฟล์จริง (T-104, 19 ก.ย. 2569)**: Data Dict 13 ฟิลด์ — `min`/`min_name` รหัส/ชื่อกระทรวง, `agc`/`agc_name` รหัส/ชื่อหน่วยงาน, `group_budget` กลุ่มงบประมาณ, `plan_name` แผนงาน, `output_name` ผลผลิต/โครงการ, `act_name` กิจกรรม, `objc` งบรายจ่าย, `objc_8_name` หมวดรายจ่ายหลัก, `cap_ncap` ประจำ/ลงทุน, `item_name` รายการ, `p_total_bud` งบประมาณ (บาท)
  - **33 กระทรวง** (รวม pseudo-ministry ครบ: งบกลาง 90000, จังหวัดและกลุ่มจังหวัด 70000, รัฐวิสาหกิจ 50000, กองทุนและเงินทุนหมุนเวียน 80000, รัฐสภา 27000, ศาล 28000, องค์กรอิสระ 29000, ส่วนราชการไม่สังกัดฯ 25000, สภากาชาดไทย 60000, ชดใช้เงินคงคลัง 95000, ส่วนราชการในพระองค์ 56000) · **3,289 หน่วยงาน (`agc`)**
  - รหัสยาว 5 ตัวอักษร **ต้องเก็บเป็น string** — `agc` ของ อปท. (กลุ่ม 75000) มีตัวอักษรปน เช่น `7510A`, `7511E`
  - data quality: `min` 80000 มี 2 ชื่อ (`กองทุนและเงินทุนหมุนเวียน` / `ทุนหมุนเวียน`); `agc` 80808 ใช้ร่วมกัน 35 ชื่อกองทุน (canonical = ชื่อที่พบบ่อยสุด ที่เหลือเป็น alias)
  - อัตรา map ชื่อ PBO → รหัส A2 (distinct คู่ กระทรวง×หน่วยงาน): 2566 = ministry 100 % / agency 98.4 % (734 คู่) · 2568 = 100 % / 98.8 % (2,809 คู่); ที่ map ไม่ได้ส่วนใหญ่คือหน่วยงานที่ไม่มีใน พ.ร.บ. 2570 (เทศบาลบางแห่ง, รัฐวิสาหกิจ, องค์การมหาชน) ไม่ใช่ปัญหาตัวสะกด
  - **ข้อควรระวัง**: fuzzy ≥ 92 จับชื่อ อปท. ผิดตัวได้ (`เทศบาลตำบลคลองโยง`→`…คลองยาง` 94.1) → ตัดสินใจ: **ปิด fuzzy สำหรับชื่อ อปท.** (ขึ้นต้น `เทศบาล`/`องค์การบริหารส่วน`) ใช้ exact/alias เท่านั้น (ทำใน T-105)

### A3. ชุด "เฉพาะจังหวัด" ปี 2569/2570 (Excel เล็ก, ยืนยันโครงสร้างแล้ว 1 ไฟล์)
- `ร่าง พ.ร.บ. งบ 2570 เฉพาะส่วนราชการที่มีรายการในเชียงใหม่ - Excel.xlsx` — sheet `รายการ (ไม่รวม อปท.)` 983 แถว; 2 แถวแรกเป็น title (“575 รายการ | งบรวม 9,043,395,000 บาท”), header อยู่แถว 3: `กระทรวง | หน่วยงาน | แผนงาน | รายการ (item_name) | ประเภทรายจ่าย | ประจำ/ลงทุน | งบ (บาท)`
- **ยืนยันครบ 6 ไฟล์ (T-106, 19 ก.ย. 2569)**: มีไฟล์เดียวที่เป็น *format A* (ไฟล์เชียงใหม่ข้างบน: title + header ไทย; พื้นที่ข้อมูล 980 แถว = 575 รายการ + 404 แถวว่าง + 1 แถว "รวมทั้งหมด" → ต้องข้ามแถวที่ `รายการ` ว่าง มิฉะนั้นยอดเบิ้ล). อีก 5 ไฟล์เป็น *format B*: sheet `Data` (ไฟล์เทศบาลใช้ชื่อ `เทศบาลในเชียงใหม่`), header แถว 1 เป็น field code แบบ A2, **ไม่มี title → ไม่มี oracle** (V2 = `no_oracle`); ไฟล์เทศบาล/อบต. เชียงใหม่ใช้ `objc_8` แทน `objc_8_name` และมีคอลัมน์ `จังหวัด/อำเภอ/ตำบล`
- ผลจริง: ส่วนราชการ→เชียงใหม่ 575 / 9,043,395,000 บาท (ตรง title เป๊ะ) · ส่วนราชการ→สมุทรปราการ 155 / 6,075,051,600 · เงินอุดหนุน อบจ. สป. 3 / 22,223,100 · อบจ. ชม. 17 / 839,827,200 · ทน. ชม. 21 / 618,267,900 · เทศบาล+อบต. ชม. 2,679 / 6,446,149,100 — **ทุกแถว match กลับ A2 ได้ 100 %** (key = agc + item_name หลัง clean + amount) → ติด flag `subset_of_act_2570_draft` ห้ามนับซ้ำใน catalog
- A2 ฉบับเต็ม: 96,470 แถว รวม **3,788,000,000,000 บาท** พอดี; `min`/`agc` เป็น string ทุกแถว; ไฟล์หลักตาม dedupe = สำเนาใต้ `งบประมาณ สมุทรปราการ/`
- ข้อควรระวังที่พิสูจน์แล้ว: filter A2 ด้วย substring ชื่อจังหวัดใช้ไม่ได้ตรง ๆ — "เชียงใหม่" ติด อ.ศรีเชียงใหม่ จ.หนองคาย (680 vs 575 แถว); "สมุทรปราการ" ติดรายการของ อปท. 2 แถว (157 vs 155)
- เป็น subset ของ A2 (filter ด้วยชื่อจังหวัดใน item_name) → ใช้เป็น **test oracle** ของ pipeline ได้: filter A2 แล้วต้องได้ยอดรวมตรงกับ title

### A4. ข้อบัญญัติ/เทศบัญญัติ อปท. ปี 2570 ที่ถอดเป็นตารางแล้ว (Sheets.xlsx)
| ไฟล์ | โครงสร้าง |
|---|---|
| `อบจ. เชียงใหม่ - Sheets.xlsx` (0.1 MB) | sheet `Data`: `REF_DOC_Page, Divison, Budgetary_Plan, Budgetary_USER, Budgetary_TYPE, Budgetary_TYPE2, Pre Project, Project, number, amount, Division, FCY` — มี **เลขหน้าอ้างอิง PDF** |
| `อบต. ราชาเทวะ - Sheets.xlsx` (0.5 MB) | 1 sheet ต่อแผนงาน (11 แผนงาน) + `ocr_raw_data` + `pivot_ocr_raw_data` + `summary_ocr_raw_data`; คอลัมน์: `page, plan, work, budget_group, expense_category, sub_category, item, amount, department, description, legal_reference` — **คุณภาพสูงสุดสำหรับ citation** (มีคำอธิบาย + ระเบียบที่อ้าง + เลขหน้า) แต่มี OCR typo (เช่น "เบี้ยยังขีพ", "ค่าครองซีพ") ต้อง fuzzy |
| `อบจ. สมุทรปราการ - Sheets.xlsx` (0.2 MB) | **ยืนยันแล้ว (T-107): ไม่เหมือน อบจ. ชม.** — 4 sheets คนละทรง; ใช้เฉพาะ `โครงการรวม งบ 70` → 574 รายการ / 3,099,999,797 บาท (1 แถวไม่มีชื่อโครงการถูกข้าม; ไม่มีเลขหน้าอ้างอิง); sheet ของปี 69 ไม่นำเข้า |
| `ทน. เชียงใหม่ - Excel.xlsx` (0.1 MB) | **ยืนยันแล้ว**: `ชีต1` 700 รายการ / 1,995,000,000 บาท — ตรงกับ sheet `_ตรวจกระทบยอด` ของต้นทางเป๊ะ; คอลัมน์ `Budgetary_*` แบบ อบจ. ชม.; `REF_DOC_Page` รูป `"N/M"`; `REF_PDF_Page` ว่างทั้งไฟล์ |

ผลจริง (T-107, 19 ก.ย.): ราชาเทวะ 345 แถว / 613,887,402 บาท · อบจ. ชม. 1,027 แถว / 2,761,133,500 บาท (มีทั้งคอลัมน์ `Divison` ว่างล้วน และ `Division`; `FCY` **ไม่ใช่ปีงบ** เป็นโน้ต/ชื่อชุมชน) · รวม A4 = 2,646 แถว. ราชาเทวะ: ยอดตามเอกสาร (`summary_ocr_raw_data`) 615,172,000 ต่างจากผลรวมรายการ 0.21 % และไม่ตรง 10/38 กลุ่ม → ADR-005

หมายเหตุ: ราชาเทวะ sheet `แผนงานงบกลาง` มีค่า amount `2` สำหรับ "เงินสมทบกองทุนประกันสังคม" (น่าจะ OCR ผิด) → validation ต้อง flag outlier

### A5. เอกสาร กมธ.ติดตามงบ (ครั้งที่ 2–14, พ.ค.–ส.ค. 2569) — Excel/XLS/docx/pptx (**ยืนยันจริง T-108, 19 ก.ย. 2569**)
- 26 ไฟล์ xlsx/xls: **มีไฟล์เดียวที่ map เป็น `budget_lines` ได้** — `กองทุนอนุรักษ์พลังงาน/1. ข้อมูลโครงการ/รวมข้อมูลโครงการ 61-68…xlsx` → dataset `committee_table` **3,325 แถว** (9 sheets, ปีงบ 2561–2565 จากชื่อ sheet, หน่วยบาท; อปท. 2,549 / ส่วนกลาง 776 แถว; แถว "รวม" 3 แถวถูกตัด; sheet "ปี 68" ไม่นำเข้าเพราะไม่พบหน่วยเงิน — ห้ามเดา); `ministry` ว่างทั้งชุด (ไฟล์ไม่ระบุ)
- อีก 25 ไฟล์ (`องค์การคลังสินค้า/Bis*`, `องค์การโคนม/BI*.XLS`, `กรมวิชาการเกษตร`, `ปศุสัตว์`, `กรมบัญชีกลาง/3.งบรายจ่าย.xlsx`) เป็นแบบฟอร์มพิมพ์จากระบบ BIS (sheet `mask*`/`sheet*` ไม่มี header ที่ map ได้) → เก็บเป็น `DocChunk` (ตารางเป็นข้อความ) ไม่มี budget_lines
- `องค์การโคนม/BI*.XLS` 13 ไฟล์: เนื้อไฟล์เป็น **xlsx (zip)** แต่ตั้งนามสกุล `.XLS` — openpyxl ปฏิเสธจาก**นามสกุล** ถ้าส่ง path → ต้องเปิดผ่าน file object (`open(path,"rb")`); เลือก reader จาก magic bytes
- `กรมบัญชีกลาง/4.งบกลาง.docx` → 1 DocChunk (ตารางงบกลาง หน่วยล้านบาท) · `ฝาย จ.พิจิตร/…pptx` → 25 DocChunks (สไลด์ 2–17); รวม docs จากชุดนี้ 28 ไฟล์ ≈ 357 KB; gzip deterministic (sha256 เท่ากันสองรอบ)

## B. PDF — สถานะ text layer

**ผลตรวจครบทุกไฟล์ (pypdf, 3 หน้าแรก, เกณฑ์ ≥ 200 ตัวอักษร/หน้า; 19 ก.ย. 2569): 262 PDF → มี text layer 119, ไม่มี 143, probe ล้มเหลว 0** — รายชื่อเต็มใน appendix. ตารางด้านล่างคือ 10 ไฟล์ตัวอย่างที่เปิดดูด้วยมือตอนวางแผน

| ไฟล์ | หน้า | text layer | ใช้ได้ |
|---|---|---|---|
| `OPEN SSO/1.x ผลเบิกจ่ายปี 2563-2567.pdf` (5 ไฟล์, 10 หน้า/ไฟล์) | 10 | **มี** (9k chars/3 หน้า) | ✅ extract ตาราง |
| `กมธ. ครั้งที่ 4/NDLP/2_ราคากลาง21.pdf` | 5 | **มี** | ✅ ราคากลาง — มีค่ามาก |
| `ร่าง พ.ร.บ. 2570 ฉบับเต็ม - PDF/3-08 มหาดไทย.pdf` | 924 | ไม่มี (274 chars = เลขหน้า) | ❌ แต่มี Excel A2 แทน |
| `.../5 ภาวะเศรษฐกิจและการคลัง.pdf` | 952 | ไม่มี | ❌ (ใช้ web search/สนช. แทน) |
| `ร่างข้อบัญญัติ อบจ. เชียงใหม่ - PDF.pdf` (199 MB) | 692 | ไม่มี | ❌ แต่มี Sheets A4 แทน (อ้างเลขหน้าได้) |
| `ร่างข้อบัญญัติ อบต. ราชาเทวะ - PDF.pdf` | 166 | ไม่มี | ❌ มี Sheets A4 แทน |
| `NDLP/1_TOR...pdf`, `สตช./โครงการเครื่องรับ-ส่งวิทยุ.pdf`, `ปศุสัตว์/PPT...pdf`, `สำนักงบประมาณ/งบประมาณ ปี 70.pdf` | – | ไม่มี | ❌ metadata only |

**ผล extract จริง (T-109, 19 ก.ย. 2569)**: PDF ที่มี text layer 119 − ซ้ำ 5 = **114 ไฟล์ สำเร็จทั้งหมด** (ไม่มี > 100 MB): 9,428 หน้า → มี text 8,309 / ไม่มี text 1,119 (ข้าม ไม่ OCR), 28,163 chunks, 7,080 ตาราง, รวม `docs/*.json.gz` **6.38 MB**. ตาราง OPEN SSO (label/value เหลื่อมแถว) และ `2_ราคากลาง21.pdf` (ราคากลาง 2 ความหมาย) **ไม่ map เป็น `committee_table`** — เก็บเป็น DocChunk + ตารางดิบ (ห้ามเดา)
คุณภาพ text ไทย (main thread ตรวจเอง): ฟอนต์ PUA (U+F700–F71A) ถูก map กลับแล้ว; แต่ยังมี **สระ/วรรณยุกต์หลุดตำแหน่ง** ~18,800 จุดใน 78 ไฟล์ (≈ 7.5 % ของตำแหน่งสระอำ) เช่น `ส านักงาน`, `จ านวน` — **ไม่ซ่อม** เพราะรูปเดียวกันมีเคสที่ไม่ใช่สระอำ (`ล านบาท` = ล้านบาท, `อ่ าศยั` = อาศัย) การแทนแบบเหมารวมจะสร้างคำผิด → T-204 (search) ต้องทำ folding แบบสมมาตรทั้ง query และ index (ตัดช่องว่างระหว่างอักษรไทย + พับ `ำ`→`า`) โดย**ไม่แตะ text ที่ใช้เป็นหลักฐาน**

ข้อสรุป: **PDF ส่วนใหญ่เป็นภาพสแกน** → ตาม scope ไม่ OCR แต่ pipeline ต้อง:
1. สแกนทุก PDF (262 ไฟล์) ด้วย pypdf (เครื่อง dev ไม่มี poppler ครบ) → บันทึก `has_text_layer`, `pages`, `size` ลง `sources.json`
2. PDF ที่มี text layer → extract เป็น text chunks ต่อหน้า (สำหรับ RAG แบบ keyword) และตาราง (pdfplumber) ถ้าตรวจพบ
3. PDF ที่ไม่มี → เก็บเฉพาะ metadata + path + ชื่อเรื่อง/หน่วยงาน/วันที่ที่ parse จากชื่อโฟลเดอร์ ให้ AI "รู้ว่ามีเอกสารนี้" และบอกผู้ใช้ให้ไปเปิดเอง

ไฟล์ใหญ่ที่ควรข้ามการอ่านเนื้อหาแม้จะมี text (ให้เก็บ metadata อย่างเดียว ถ้า > 100 MB): `สัญญา TH-AI Passport` 98.9 MB, `_เลือกตั้ง ผอ.` 63 MB, ข้อบัญญัติ 5 ไฟล์ 100–260 MB

## C. โครงสร้างโฟลเดอร์ → metadata

- `กมธ.ติดตามงบ/ครั้งที่ N (วัน เดือน ปี)/<เรื่อง>/<หน่วยงาน>/ไฟล์` → parse `meeting_no`, `meeting_date` (พ.ศ.), `topic`, `agency`
  ระวัง: ชื่อโฟลเดอร์มี **zero-width space (U+200B)** เช่น `ครั้งที่​ 10` — ต้อง normalize unicode ก่อน regex
- `งบประมาณ <จังหวัด>/<ลำดับ> - <ระดับ>/ไฟล์` → `province`, `level` (ฟังก์ชัน/อบจ./เทศบาล/อบต.)
- `OPEN SSO/` → `agency = สำนักงานประกันสังคม`, ปีจากชื่อไฟล์

## D. ไฟล์ที่ไม่ใช้
- `.DS_Store`, `.jpg` 4 ไฟล์ (รูป), `องค์การโคนม/สิ่งที่ส่งมาด้วย 1 แผนแก้ไขสถานการณ์ อสค ณ 22เม.ย.69` (ไม่มีนามสกุล — ให้ตรวจ magic bytes; ถ้าเป็น PDF ให้ปฏิบัติเหมือน PDF)

## E. ประมาณการขนาดหลัง compact `[UNVERIFIED — ให้ pipeline วัดจริงและอัปเดตหัวข้อนี้]`
- PBO 2.9 M แถว → Parquet (zstd, dictionary-encoded ชื่อกระทรวง/หน่วยงาน/แผนงาน) ≈ 150–250 MB รวม → แบ่ง shard ปี × กระทรวง ให้ทุกไฟล์ < 24 MB
- Catalog (item_key aggregate) ≈ 2–6 MB gz
- sources.json = **333 KB (วัดจริง 19 ก.ย. — อาจเปลี่ยนเล็กน้อยหลังแก้ rel_path เป็น posix)**
